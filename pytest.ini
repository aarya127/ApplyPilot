[pytest]
markers =
    slow: Playwright-based tests that launch a browser (skip with -m "not slow")
    integration: Tests that require live network access
testpaths = tests
; Offline-safe by default; run live-site tests explicitly with:
;   pytest -m integration   (or -m "integration or slow")
addopts = -m "not integration and not slow"
